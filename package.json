{
  "name": "markboard",
  "version": "1.0.0",
  "private": true,
  "description": "A plain-text project tracker powered by Markdown",
  "license": "MIT",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "test:all": "vitest run && playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "serve": "^14.2.6",
    "vitest": "^3.0.0"
  }
}
